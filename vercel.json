{
  "buildCommand": "npx prisma generate && npx prisma db push && cd abaia-frontend && npm install && npm run build && rm -rf ../public && cp -r dist ../public",
  "outputDirectory": "public",
  "routes": [
    { "src": "/api/(.*)", "dest": "api/index.js" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
